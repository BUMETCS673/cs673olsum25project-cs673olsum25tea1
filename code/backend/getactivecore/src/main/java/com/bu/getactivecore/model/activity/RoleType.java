package com.bu.getactivecore.model.activity;

/**
 * * Represents the type of role a user can have in an activity.
 */
public enum RoleType {
	/**
	 * Represents a user with administrative privileges in an activity.
	 */
	ADMIN,

	/**
	 * Represents a user with participant privileges in an activity.
	 */
	PARTICIPANT
}